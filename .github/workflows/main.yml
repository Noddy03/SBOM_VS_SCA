name: Fetch Top IoT Repositories + Trivy/Conan SBOM Scan (NTIA + BSI V2)

on:
  workflow_dispatch:
    inputs:
      language:
        description: "Programming language to scan (e.g. C++, Python, Java)"
        required: true
        default: "C++"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  fetch_top_repos:
    name: Fetch IoT repositories
    runs-on: ubuntu-latest
    outputs:
      repo_list: ${{ steps.fetch.outputs.repo_list }}

    steps:
      - name: Install jq
        run: sudo apt-get update -y && sudo apt-get install -y jq

      - name: Fetch top IoT repos
        id: fetch
        env:
          GH_TOKEN: ${{ github.token }}
          LANG: ${{ github.event.inputs.language }}
        run: |
          RAW_Q="topic:IoT language:\"$LANG\" fork:false archived:false"

          REPOS=$(gh api /search/repositories \
            --raw-field q="$RAW_Q" \
            --raw-field sort=stars \
            --raw-field order=desc \
            --raw-field per_page=20 \
            --jq '.items[].full_name')

          JSON=$(echo "$REPOS" | jq -R -s -c 'split("\n")[:-1]')
          echo "repo_list=$JSON" >> "$GITHUB_OUTPUT"

  sbom_scan:
    name: SBOM Scan
    needs: fetch_top_repos
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        repo: ${{ fromJson(needs.fetch_top_repos.outputs.repo_list) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: ${{ matrix.repo }}

      - name: Install Conan, Trivy, jq, curl
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq curl wget python3-pip python3-venv

          pip install conan

          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sudo sh -s -- -b /usr/local/bin

      - name: Generate SBOMs
        run: |
          mkdir -p sbom

          trivy fs . --format cyclonedx --output sbom/cyclonedx.json || true

          if [ -f "conanfile.txt" ] || [ -f "conanfile.py" ]; then
            conan graph info . --format=json > sbom/conan-sbom.json || true
          fi

      - name: Sanitize repo name
        run: |
          SAFE_REPO="${{ matrix.repo }}"
          SAFE_REPO="${SAFE_REPO//\//_}"
          echo "SAFE_REPO=$SAFE_REPO" >> $GITHUB_ENV

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ env.SAFE_REPO }}
          path: sbom/
